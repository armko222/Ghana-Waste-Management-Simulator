package com.dcit308.wasteops.db;

/**
 * Reads data/csv/*.csv, validates rows, writes into the database via the
 * repository classes. Reject malformed rows clearly rather than
 * skipping them silently.
 *
 * Owned by Issue #1.
 */
public class CsvImporter {

    public void importAll(String csvDirectoryPath) {
        throw new UnsupportedOperationException("TODO: Issue #1 \u2014 implement CSV import for all four files.");
    }
}
